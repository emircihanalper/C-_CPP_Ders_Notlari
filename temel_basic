https://cpp.sh/ >> online c++ kodlayıcısı. Yüklemeden buradan kod yazıp koşturabilirsiniz. 

#include <iostream> 	//ile kütüphane dahil edildi.
using namepscae std; 	//ile kütüphanedeki objeleri ve değişkenleri kullanmak için
				//bu iki satır ile kütüphaneyi dahil ediliyor

int main() 			//tüm c++ yazılımları bu satır ile başlar!!!!
{
cout <<"merhaba"; 	//cout ekrana yazı vermek çin gerekli kod.

return 0; 			//return 0 ile c++ yazılımı bitiritoruz. MECBUR ana program int olduğu için!!!!
}
---------------------------------------------------
\n yi cout kısmında en sona yazarsak; bu komut ile ekran çıktısını alt satıra verecek. diğer türlü tüm cout kodları yan yana yazılmaktadır. Enter tuşuna basılmış gibi.

cout<<"merhaba\n"; 	//şekinde kullanılır.
------VEYA-----------------------------------
<<endl komutu ile aynı işlemi yapabiliriz.

cout<<"merhaba"<<endl;
-----------------------------------------------------
\t				// TAB tuşu gibi bşluk bırakır
\\				// ekrana tek \ karakterini vermek için
\"				// ekrana tek " karakterini vermek için
-----------------------------------------------------------
// 				// yorum satırı haline getiriyor
/*  */			// * arasındaki tüm satırlar yorum satırı olur
--------------------------------------------------------
aynı değerde birçok faklı değeri "," ile ayırabiliriz.

int a=10, b=20, c=30;	// gibi tanımlanabilir.
----------------------------------------------------------------------------------
cin>> 			// ekrandan veri girişi için kullanılır.
---------------------------------------------------------------------------------
() içindeki ifade şart sağlandığı zaman 1, değilse 0  yazar

//klavyeden girilen sayı çiftse 0, değilse 1 yazsın olsun
int say1;
cin>>say1;
cout<<(say1%2); 		//çıktı sadece 1 veya 0 olur.
